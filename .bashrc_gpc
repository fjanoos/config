# -- gpc specific bash configurations ----

export PORTAL=10.128.0.2
export PYTHONPATH='/home/fjanoos/cloud'
export JUPYTER_PATH=$PYTHONPATH:$JUPYTER_PATH
alias notebook='jupyter notebook --no-browser'

# run rcloud for mounting (see https://rclone.org/commands/rclone_mount/)
alias gdrive='rclone mount fredjames586:cloud /mnt/local/cloud --vfs-cache-mode full --daemon --dir-cache-time 1s --attr-timeout 1s'

